pipeline{
	agent any
	environment{
		RELEASE= 1.3
	}

	stages{
		stage{
			agent any
			environment{
				LOG_LEVEL= INFO
			}
			steps{
				sh "echo Release ${RELEASE} for LogLevel ${LOG_LEVEL}"
			}
		}
		stage{
			steps{
			sh "echo Release ${RELEASE} for LogLevel ${LOG_LEVEL}"
			}
		}
	}
}
